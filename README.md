# requirements-implementation-differences
Agent which detects differences between requirements and implementation, detects missing tests, proposes tests and generates questions to clarify the differences between the requirements and the implementation
